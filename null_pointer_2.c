#include <stdio.h>
int main() {
    int *ptr = NULL;  // initialize pointer to NULL
    if (ptr == NULL)
   {
        printf("Pointer is NULL, not pointing to any memory.\n");
   }
    return 0;
}
